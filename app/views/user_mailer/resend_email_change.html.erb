<div>
  <h1><%= t('mailer.confirm.welcome')%> <%= @user.name %> <%= t('mailer.confirm.to')%></h1>

  <p style="font-size: 20px"><%= t('mailer.confirm.content')%></p>

  <h2 style="font-size: 20px"><%= t('mailer.confirm.code')%><%= @user.confirmation_token %></h2>
  <h4 style="font-size: 20px"><%= t('mailer.confirm.thank_you')%></h4>
  <h4 style="font-size: 20px"><%= t('mailer.confirm.team_qakha')%></h4>
</div>
