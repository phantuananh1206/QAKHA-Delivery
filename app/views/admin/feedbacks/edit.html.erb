<%= provide :title, t('admin.feedback.edit_feedback') %>
<%= provide :button, t('admin.feedback.edit_feedback') %>
<%= provide :url, admin_feedback_path(@feedback)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= t('admin.feedback.edit_feedback') %></h1>
    <%= render "form" %>
  </div>
</div>
