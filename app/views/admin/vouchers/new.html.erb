<%= provide :title, "New Voucher"%>
<%= provide :button, "Create Voucher"%>
<%= provide :url, admin_vouchers_path(@voucher)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= "Create new voucher" %></h1>
    <%= render "form" %>
  </div>
</div>
