<%= provide :title, t('admin.voucher.edit_voucher') %>
<%= provide :button, t('admin.voucher.edit_voucher') %>
<%= provide :url, admin_voucher_path(@voucher)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= t('admin.voucher.edit_voucher') %></h1>
    <%= render "form" %>
  </div>
</div>
