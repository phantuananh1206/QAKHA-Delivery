<%= provide :title, t('admin.user.edit_user') %>
<%= provide :button, t('admin.user.edit_user') %>
<%= provide :url, admin_user_path(@user)%>

<div class="custom-form">
  <div class="col-md-6 col-md-offset-3">
    <h1 class="text-center"><%= t('admin.user.edit_user') %></h1>
    <%= render "form_edit" %>
  </div>
</div>
<%= link_to t('back'), admin_users_path, class:'btn btn-secondary glyphicon glyphicon-arrow-left' %>
