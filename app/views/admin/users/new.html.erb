<%= provide :title, t('admin.user.create_user') %>
<%= provide :button, t('admin.user.create_user') %>
<%= provide :url, admin_users_path(@user)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= t('admin.user.create_user') %></h1>
    <%= render "form" %>
  </div>
</div>
