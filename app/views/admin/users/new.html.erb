<%= provide :title, "New User"%>
<%= provide :button, "Create User"%>
<%= provide :url, admin_users_path(@user)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= "Create new user" %></h1>
    <%= render "form" %>
  </div>
</div>
