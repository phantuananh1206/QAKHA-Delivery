<%= provide :title, t('admin.type.create_type') %>
<%= provide :button, t('admin.type.create_type') %>
<%= provide :url, admin_types_path(@type)%>

<div class="custom-form">
  <div class="col-md-6 col-md-offset-3">
    <h1 class="text-center"><%= t('admin.type.create_type') %></h1>
    <%= render "form" %>
  </div>
</div>
<%= link_to t('back'), admin_types_path, class:'btn btn-secondary glyphicon glyphicon-arrow-left' %>
