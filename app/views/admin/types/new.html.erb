<%= provide :title, "New Type"%>
<%= provide :button, "Create Type"%>
<%= provide :url, admin_types_path(@type)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= "Create new type" %></h1>
    <%= render "form" %>
  </div>
</div>
