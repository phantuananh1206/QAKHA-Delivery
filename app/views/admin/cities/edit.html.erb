<%= provide :title, t('admin.city.edit_city') %>
<%= provide :button, t('admin.city.edit_city') %>
<%= provide :url, admin_city_path(@city)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= t('admin.city.edit_city') %></h1>
    <%= render "form" %>
  </div>
</div>
