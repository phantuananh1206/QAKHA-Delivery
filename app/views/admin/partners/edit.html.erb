<%= provide :title, t('admin.partner.edit_partner') %>
<%= provide :button, t('admin.partner.edit_partner') %>
<%= provide :url, admin_partner_path(@partner)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= t('admin.partner.edit_partner') %></h1>
    <%= render "form" %>
  </div>
</div>
