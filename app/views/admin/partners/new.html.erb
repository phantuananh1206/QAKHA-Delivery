<%= provide :title, t('admin.partner.create_partner') %>
<%= provide :button, t('admin.partner.create_partner') %>
<%= provide :url, admin_partners_path(@partner)%>

<div class="custom-form">
  <div class="col-md-6 col-md-offset-3">
    <h1 class="text-center"><%= t('admin.partner.create_partner') %></h1>
    <%= render "form" %>
  </div>
</div>
<%= link_to t('back'), admin_partners_path, class:'btn btn-secondary glyphicon glyphicon-arrow-left' %>
