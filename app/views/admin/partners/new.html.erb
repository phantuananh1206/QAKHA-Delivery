<%= provide :title, "New Partner"%>
<%= provide :button, "Create partner"%>
<%= provide :url, admin_partners_path(@partner)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= "Create new partner" %></h1>
    <%= render "form" %>
  </div>
</div>
