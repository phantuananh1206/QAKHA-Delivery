<%= provide :title, t('admin.product.edit_product') %>
<%= provide :button, t('admin.product.edit_product') %>
<%= provide :url, admin_product_path(@product)%>

<div class="">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= t('admin.product.edit_product') %></h1>
    <%= render "form" %>
  </div>
</div>
