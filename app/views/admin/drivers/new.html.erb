<%= provide :title, t('admin.driver.create_driver') %>
<%= provide :button, t('admin.driver.create_driver') %>
<%= provide :url, admin_drivers_path(@driver)%>

<div class="custom-form">
  <div class="col-md-6 col-md-offset-3">
    <h1 class="text-center"><%= t('admin.driver.create_driver') %></h1>
    <%= render "form" %>
  </div>
</div>
<%= link_to t('back'), admin_drivers_path, class:'btn btn-secondary glyphicon glyphicon-arrow-left' %>
