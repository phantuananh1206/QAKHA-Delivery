<%= provide :title, "Update voucher"%>
<%= provide :button, "Update"%>
<%= provide :url, partners_voucher_path(@voucher)%>
<div class="custom-form">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= "Update voucher" %></h1>
    <%= render "form" %>
  </div>
</div>
<%= link_to t('back'), partners_vouchers_path, class:'btn btn-secondary glyphicon glyphicon-arrow-left' %>
