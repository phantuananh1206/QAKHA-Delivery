<%= provide :title, t('admin.voucher.create_voucher') %>
<%= provide :button, t('admin.voucher.create_voucher') %>
<%= provide :url, partners_vouchers_path(@voucher)%>
<div class="custom-form">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= t('admin.voucher.create_voucher') %></h1>
    <%= render "form" %>
  </div>
</div>
<%= link_to t('back'), partners_vouchers_path, class:'btn btn-secondary glyphicon glyphicon-arrow-left' %>
