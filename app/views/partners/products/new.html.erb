<%= provide :title, "New Product"%>
<%= provide :button, "Create"%>
<%= provide :url, partners_products_path(@product)%>
<div class="custom-form">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= "Create new product" %></h1>
    <%= render "form" %>
  </div>
</div>
<%= link_to t('back'), partners_products_path, class:'btn btn-secondary glyphicon glyphicon-arrow-left' %>
